{
  "name": "cigh",
  "version": "0.0.1",
  "description": "Auto Github issues closer using specific time",
  "main": "index.js",
  "scripts": {
    "start": "",
    "start:dev": "nodemon ./dist/index.js",
    "dev": "run-p build:watch start:dev",
    "build": "babel src -d dist --source-maps inline",
    "build:watch": "babel src -d dist --source-maps inline -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "b-b4rry",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^2.0.0",
    "hapi": "^16.0.1",
    "mongoose": "^4.7.1"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-latest": "^6.16.0",
    "nodemon": "^1.11.0",
    "npm-run-all": "^3.1.2"
  }
}
